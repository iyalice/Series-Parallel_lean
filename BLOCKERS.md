# Current blockers

**Remaining blockers for the requested three main theorems: none.**

All gates B1–B6 are closed:

- finite path/flow semantics, deterministic substitution, and first-moment Fekete limits;
- exact density CDF formulas and generalized-inverse monotone coupling;
- internal `diffusionCoefficient = 2 * zetaThree`;
- normalized second moments, Jensen gap, center tracking, and all parameter ranges;
- weighted consistency plus global upper and hard-edge lower barriers;
- BVP least parameter, cubic reparameterization, and all three near-critical equivalences.

The canonical current status is this file. `MAIN_TEXT_BLOCKERS.md` is intentionally retained as
the frozen 2026-08-26 continuation snapshot (SHA-256
`2FE1CEB05DA512BB938056A4E663F51DB4EB59C44F76BB6C3EACFE66A041A17F`) and must not be read as
the present state.

Non-blocking source-map refinements remain explicitly marked `partial`: a few displays request
a sharper standalone two-sided cutoff constant than the exported theorem-path inequality.
They do not weaken or enter any of the three complete source-facing theorem statements.
